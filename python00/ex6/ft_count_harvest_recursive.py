def ft_count_harvest_recursive() -> None:
    days = int(input("Days until harvest: "))
    for day in range(helper_recursive(actual_day)):

def helper_recursive(int: actual_day; int: final_day)
    if (actual_day >= )

ft_count_harvest_recursive()
